import React from 'react';

import { GameOverSC } from './StyledComponents';

export default () => (
  <GameOverSC>
    <div>GAME OVER</div>
    <br />
    <span>press SPACE to reset</span>
  </GameOverSC>
);
